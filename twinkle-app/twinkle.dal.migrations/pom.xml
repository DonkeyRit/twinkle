<?xml version="1.0"?>
<project
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
  xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

  <modelVersion>4.0.0</modelVersion>
  <artifactId>dal.migrations</artifactId>
  <version>1.0-SNAPSHOT</version>
  <name>dal.migrations</name>

  <!-- mvn clean flyway:info
  -Ddatabase.url=jdbc:postgresql://127.0.0.1:5433/integration_tests_carrental
  -Ddatabase.user=twinkle_test_user -Ddatabase.password=Sr412Tqew! -X -->

  <parent>
    <artifactId>root</artifactId>
    <groupId>com.github.donkeyrit.twinkle</groupId>
    <version>1.0-SNAPSHOT</version>
  </parent>

  <build>
    <plugins>
      <plugin>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-maven-plugin</artifactId>

        <executions>
          <execution>
            <id>flyway-migrate</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>migrate</goal>
            </goals>
          </execution>
        </executions>

        <configuration>

          <url>${database.url}</url>
          <user>${database.user}</user>
          <password>${database.password}</password>
          <schemas>
            <schema>public</schema>
          </schemas>
          <cleanDisabled>false</cleanDisabled>

          <locations>
            <location>filesystem:src/main/resources/db/migrations</location>
          </locations>
        </configuration>

        <dependencies>
          <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>${postgresql.version}</version>
          </dependency>
        </dependencies>
      </plugin>
      <!--<plugin>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-maven-plugin</artifactId>
        
        <executions>
          <execution>
            <id>flyway-migrate</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>migrate</goal>
            </goals>
          </execution>
        </executions>
        
        <configuration>
    
          <url>jdbc:postgresql://127.0.0.1:5433/integration_tests_carrental?ssl=false</url>
          <user>twinkle_test_user</user>
          <password>Sr412Tqew!</password>
          <locations>
              <location>filesystem:src/main/resources/db/migrations</location>
          </locations>
      </configuration>

        <dependencies>
          <dependency>
              <groupId>org.postgresql</groupId>
              <artifactId>postgresql</artifactId>
              <version>42.7.2</version>
          </dependency>
      </dependencies>
      </plugin>-->
    </plugins>
  </build>
</project>